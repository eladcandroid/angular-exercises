import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-bonus-storybook',
  templateUrl: './bonus-storybook.html',
  styleUrl: './bonus-storybook.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class BonusStorybookComponent {}
